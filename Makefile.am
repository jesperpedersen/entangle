
SUBDIRS = src

# XXX figure out why these aren't detected by git.mk
GITIGNOREFILES = aclocal.m4 capa-*.tar.gz compile config.guess config.h.in config.sub depcomp install-sh missing

-include $(top_srcdir)/git.mk
